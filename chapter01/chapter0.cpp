#include <iostream>

int main()
{
    // Chapter 0 snippets
    std::cout << "Hello World!" << std::endl;
    std::cout << "This (\") is a quote, and this (\\) is a backslash." << std::endl;
    std::cout << "Tab \t test" << std::endl;

    return 0;
}

// Chater 0 notes
// Namespace: used to prevent name collisions, provides a scope for the names of functions, types, variables, etc
// Headers: pre-existing headers and user-defined headers; contain function definitions, data type definitions, macros
// Scope: portion of the source code where a name is valid
// Expression: operators, constants, variables arranged according to the syntax rules
// Statement: controls how and in what order objects are manipulated
// Output: program output

